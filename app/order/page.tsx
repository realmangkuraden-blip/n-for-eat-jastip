'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function OrderLookup(){const[order,setOrder]=useState(''),[phone,setPhone]=useState(''),router=useRouter();return <main className="container py-16"><div className="mx-auto max-w-xl card p-8"><h1 className="text-3xl font-black">Lacak Pesanan</h1><p className="mt-2 opacity-70">Masukkan nomor order dan nomor WhatsApp yang digunakan saat checkout.</p><form className="mt-6 space-y-4" onSubmit={e=>{e.preventDefault();sessionStorage.setItem(`nfe-phone-${order.toUpperCase()}`,phone);router.push(`/order/${order.toUpperCase()}`)}}><label className="block text-sm font-bold">Nomor Order<input required placeholder="NFE-20260916-1234" className="mt-2 w-full rounded-xl border p-3 font-normal" value={order} onChange={e=>setOrder(e.target.value)}/></label><label className="block text-sm font-bold">Nomor WhatsApp<input required className="mt-2 w-full rounded-xl border p-3 font-normal" value={phone} onChange={e=>setPhone(e.target.value)}/></label><button className="btn btn-primary w-full">Lacak Pesanan</button></form></div></main>}
